# One or more HTTP(S) sources, separated by literal newlines.
# Only configure sources whose terms permit your intended use.
SOURCE_URLS=https://example.org/permissively-licensed-list.txt
HTTP_TIMEOUT=60

